{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Adresse list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse</th>
                <th>Mail</th>
                <th>Tel</th>
                <th>Fix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('equipe_adresse_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.adresse }}</td>
                <td>{{ entity.mail }}</td>
                <td>{{ entity.tel }}</td>
                <td>{{ entity.fix }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('equipe_adresse_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('equipe_adresse_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('equipe_adresse_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
